import { View, Text } from "react-native";
import React from "react";
import CandidateCameraScreen from "../components/CandidateCameraScreen";

const CaptureCandidate = () => {
  return <CandidateCameraScreen />;
};

export default CaptureCandidate;
